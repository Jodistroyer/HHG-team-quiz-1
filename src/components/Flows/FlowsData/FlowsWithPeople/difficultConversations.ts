import type { FlowSituation } from '../flowTypes'
import { makeSituation } from '../situationHelpers'

export const difficultConversationsFlowSituation: FlowSituation = makeSituation(
  'difficult-conversations',
  'Difficult conversations',
  'Say the hard thing with care and clarity.'
)
