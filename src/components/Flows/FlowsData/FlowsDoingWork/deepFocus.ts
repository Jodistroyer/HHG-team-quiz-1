import type { FlowSituation } from '../flowTypes'
import { makeSituation } from '../situationHelpers'

export const deepFocusFlowSituation: FlowSituation = makeSituation(
  'deep-focus',
  'Deep focus',
  'Get into flow and protect your attention.'
)
