import type { FlowSituation } from '../flowTypes'
import { makeSituation } from '../situationHelpers'

export const buildingHabitsFlowSituation: FlowSituation = makeSituation(
  'building-habits',
  'Building habits',
  'Make it easy to start and repeat.'
)
