import type { FlowSituation } from '../flowTypes'
import { makeSituation } from '../situationHelpers'

export const longTermChangeFlowSituation: FlowSituation = makeSituation(
  'long-term-change',
  'Long-term change',
  'Stay consistent when motivation fades.'
)
